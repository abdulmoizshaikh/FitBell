import {StyleSheet} from 'react-native';
// import styled from 'styled-components/native';

const styles = StyleSheet.create({
  wifiWidget: {
    // background-color: ${themeColors.Danger};
    // position: absolute;
    // bottom: ${Metrix.VerticalSize(120)}px;
    // right: ${Metrix.HorizontalSize(20)}px;
    // border-radius: ${Metrix.HorizontalSize(100)}px;
    // height: ${Metrix.VerticalSize(35)}px;
    // justify-content: center;
    // align-items: center;
    // width: ${Metrix.HorizontalSize(33)}px;
    // border-width: 1px;
    // border-color: ${themeColors.Dark};
  },
});

export default styles;
