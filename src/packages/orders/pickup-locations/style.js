import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    container: {
        ...StyleSheet.absoluteFillObject,
        alignItems: 'center',
        paddingHorizontal: 5,

    },
    map: {
        ...StyleSheet.absoluteFillObject,
        flex: 1
    },
})